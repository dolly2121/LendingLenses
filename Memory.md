# Memory - living progress log

**Instructions for the AI: read this file FIRST every session, before the codebase. Update it at the end of every phase and after any significant decision. Newest entries at the top. Keep entries to one or two lines.**

---

## Current status

- Current phase: 8 (item 1 of 3 complete; item 2 declined for now; item 3 handed off to the human, see Blockers)
- Last completed phase: 7 (docker compose caveat noted below)
- Gold contracts frozen: YES, as of 2026-07-15. `gold_loan_summary`: state, risk_band, loan_count, total_amount, avg_amount, last_updated (no identifiers of any class). `gold_call_insights`: call_id (I), transcript_masked, sentiment_score, hardship_flag, complaint_flag, processed_at. Both enforced by a `strict=True` pandera schema in pipeline/gold.py — any extra/direct-identifier column raises immediately. Changing either after this point requires the human to say so explicitly (Rules R7).
- Blockers: Phase 8 item 3 (Power BI Desktop) is the human's to build by hand - handed off the absolute Delta table path (`data\lake\gold\gold_loan_summary`) and connector steps (Get Data > More > Other > Delta Lake), since it needs a Windows GUI app with no automation tooling available in this session (mirrors the Docker Desktop GUI limitation below). Item 2 (faster-whisper) was explicitly declined for now, not attempted - still available to build later if wanted.
- Blockers: `docker compose up` (Phase 7 DoD item 2) is unverified in this dev environment - Docker Desktop's backend processes start but the engine pipe never comes up within ~3 minutes of waiting, likely needs interactive/GUI first-run setup this automated session can't provide. `docker-compose.yml`/both Dockerfiles were reviewed for correctness but never actually built/run. Phases.md Phase 7 itself marks this "not required live" and lists it second-to-drop under time pressure, and `make demo` (the actual non-negotiable deliverable) is fully verified - so this was not treated as blocking, but it is a real, undischarged DoD item. Try again on a machine where Docker Desktop has completed its first-run setup, or ask the human to run `docker compose up` once themselves.

## Decisions log

Format: date, decision, reason.

- 2026-07-16: Phase 8 item 1 (run summary table) complete. `pipeline/run_pipeline.py` writes one row to a new `run_summary` table at the end of every invocation - a current-state snapshot (loans/calls landed/passed/quarantined, gold row count), not a this-run delta, so it stays meaningful even after an idempotent no-op rerun. Deliberately reads final table heights after Bronze/Silver/Gold complete rather than threading return values through bronze.main()/silver.main()/gold.main(), so none of that already-verified code needed touching. `dashboard/app.py`'s Data Quality panel now shows the latest run's snapshot as its first line. Verified: two consecutive `python -m pipeline.run_pipeline` runs produced 2 accumulating run_summary rows with correct cumulative counts (500/494/6/3/3/0/24) even though Bronze/Silver/Gold themselves both printed all-zero (idempotent skip); dashboard renders the new line correctly with no regressions elsewhere; `make test` still 14/14 green.
- 2026-07-16: Resolved the long-standing `make` blocker (open since Phase 1/2): `choco install make -y` had failed for lacking admin rights, but `conda install -c conda-forge make -y` worked without needing elevation (conda installs into the user-owned anaconda env). `make --version` confirms GNU Make 4.4.1 now on PATH; `make test` and `make demo` both verified working for real (not just via the `python -m pipeline.x` workaround used in Phases 1-6).
- 2026-07-16: Phase 7 closed out (with one caveat, see Blockers above). `make test` reconfirmed green (14 passed) after all Phase 7 additions. `docker compose up` could not be verified end-to-end in this session - Docker Desktop wouldn't finish starting its engine within a bounded wait despite two attempts - so DoD item 2 is unverified, not failed. Everything else (`make demo`, `make test`, README, ARCHITECTURE_MAPPING.md, terraform/main.tf) is verified as described in the entry below.
- 2026-07-16: Phase 7 in progress. `pipeline/demo.py` sequences `make demo`: runs the full pipeline, confirms both Gold tables exist, starts the dashboard and AI service as background processes, polls `GET /health` (30s timeout) and also checks both processes are still alive before printing `DEMO ready` - "no race conditions on stage" per Phases.md. Makefile's `demo` target now runs `pip install -r requirements.txt` first so `make demo` alone is the complete "one command from a fresh clone" (PRD N1, Rules R8's 3-step quickstart: clone, one command, open browser) - added `requirements.txt` with exact pinned versions of everything currently installed and verified working together. Verified live: `rm -rf data/lake && make demo` correctly ran pip install, then Bronze/Silver/Gold in order (494 passed, 6 quarantined, 24 Gold rows), started both servers, both reported healthy, and printed `DEMO ready -> dashboard: http://localhost:8501  service: http://localhost:8000` with the exact expected console lines throughout - full transcript captured, not just a spot-check. `ai_service/Dockerfile` and `dashboard/Dockerfile` added; `docker-compose.yml` reuses the ai_service image for a one-shot `pipeline` service (avoids a third Dockerfile) that populates a bind-mounted `./data:/app/data` shared volume before dashboard/service start via `depends_on: condition: service_completed_successfully`. `terraform/main.tf`: one illustrative, unapplied `azurerm_key_vault` block, deliberately tied to the existing fixed-salt-vs-HMAC narrative (this IS where the production HMAC key would live). `ARCHITECTURE_MAPPING.md`: one section per Architecture.md section 2 table row, each closing with its scale/security/governance note, plus the fixed-salt-vs-HMAC sentence Phase 7 explicitly asked for. `README.md`: exactly 3 quickstart steps (clone, `make demo`, open browser) plus a Demo Script section with the 3 scripted moments and the exact curl command for moment 3.
- 2026-07-16: Phase 6 complete. `ai_service/nlp_flags.py`: VADER sentiment plus two keyword lists (hardship/complaint), and `mask_names` which redacts tokens matching generate_data.py's existing `FIRST_NAMES`/`LAST_NAMES` pool (reused, not duplicated) - this is the concrete meaning of "keyword based, not real NER" in Rules R3a: a fixed known-name vocabulary, not a model. `ai_service/main.py`: FastAPI `POST /process-call` (multipart `.txt` upload) and `GET /health`. Installed `vaderSentiment` (approved, not yet present); `fastapi`/`uvicorn` were already installed, and so was `python-multipart` (required by FastAPI's own `UploadFile` handling, not a separate library choice - needed to accept the file upload at all). `tests/test_nlp_flags.py` (4 tests, all green) covers the exact Phase 6 DoD wording: hardship-only, complaint-only, neither, and no raw name token survives masking on any of the 3 official transcripts. Verified live via the browser preview (uvicorn on :8000, streamlit on :8501): `/health` returns ok; posting the hardship call while the dashboard was open, then clicking Refresh, showed the flagged row with the correct red "Hardship" badge; the mandatory R4 rehearsal gate passed (5 consecutive posts, dashboard refreshed after each, all HTTP 200, zero errors in service logs); error paths return clean 400s with short messages, no stack trace, for a wrong file extension and an empty transcript (Rules R6). Schema note: polars auto-infers `datetime.now(timezone.utc)` as `Datetime("us", "UTC")` when building the insight row, so no explicit dtype cast was needed to match the frozen gold_call_insights contract from Phase 4. Reset `data/lake` to a clean baseline (494 loans, 0 calls) after rehearsal testing, since it's regenerable/gitignored. Added a `service` config to `.claude/launch.json` (uvicorn, port 8000) alongside the existing dashboard one.
- 2026-07-16: Added a third Phase 8 optional-polish item (Phases.md): rebuild the dashboard's loan summary view in Power BI Desktop (free, local), pointing at `gold_loan_summary` via the Delta connector, after the Whisper item. Streamlit remains the primary rehearsed demo path; this is a bonus if time allows. Reason: turns the "this local setup maps to Power BI" architecture claim into a literal "this is Power BI reading the same table" demonstration.
- 2026-07-15: Phase 5 complete. `dashboard/app.py` (Streamlit, no new dependencies - both `streamlit` and its bundled `altair` were already installed) renders all four Design.md rows: metric tiles, a teal bar chart (loans by state) + a donut (loans by risk band, colour-coded LOW=teal/MEDIUM=amber/HIGH=red reusing the existing palette semantics rather than inventing new colours), a call insights table with coloured HTML-span badges instead of True/False text (currently showing "No calls processed yet" since gold_call_insights is still empty pending Phase 6), and a collapsible Data Quality panel. Manual Refresh button per Rules R4/Design.md (not a timer); reads go through a 2-retry-then-fall-back-to-last-good-session-state wrapper (`_read_with_retry`), never surfacing an error to the presenter. Two scoped decisions worth flagging: (1) "quarantine total" in the quality panel is derived by summing `dq_audit.failed_count` rather than reading the `quarantine` table directly, because FR7 restricts the dashboard to "only Gold and audit tables" - for this dataset every defect trips exactly one check so the sum equals the true quarantine row count (6), but this would need revisiting if a row could ever fail multiple checks. (2) Design.md's "masked identifiers display as shortened hashes" line has no matching UI element on this dashboard, since Gold (frozen Phase 4) carries no identifier/hash columns at all - it likely describes demo moment 2, which Phase 3 already covers via console + table peek, not the dashboard. `streamlit run` (unlike the pipeline's `-m` invocation) doesn't support module execution, so `dashboard/app.py` inserts the repo root onto `sys.path` itself. Added `.claude/launch.json` (Streamlit dev server, port 8501) for local preview. Verified DoD via the browser preview: all four sections render correctly from a fresh pipeline run (494 loans, $124,300,721, 8/12 checks clean, quarantine total 6), Refresh works with no errors, and stopping + restarting the server recovers cleanly with identical data. No new test file added - Rules R8 names exactly 3 test files (silver quality, masking, NLP flags) and the dashboard isn't one of them.
- 2026-07-15: Phase 4 complete, Gold contracts frozen (see Current status above). `pipeline/gold.py` builds `gold_loan_summary` (group_by state+risk_band over silver_loans: loan_count, total_amount, avg_amount, last_updated) and initialises `gold_call_insights` empty with its agreed schema (ready for the AI service to append to in Phase 6). Both tables validated by a `strict=True` pandera schema before write — this is a self-check on our own output shape (any unexpected/direct-identifier column is a pipeline bug, so it raises rather than being quarantined, unlike silver.py's handling of untrusted input data). Same idempotency pattern as Bronze/Silver: skips entirely if the Gold table already exists, since this is a full single-batch recompute, not an incremental append. `run_pipeline.py` now runs Bronze -> Silver -> Gold in that literal order (also fixes a real bug: `from pipeline import bronze, gold, silver` had imported alphabetically, so gold.py's pandera import fired before silver.py's pandera-warning suppression was installed; also added the same warning filter directly in gold.py so it stays clean if gold.py is ever run standalone). Two dtype gotchas fixed: pandera's `Column(pl.Datetime)` defaults to a naive (no-timezone) dtype, had to use `Column(pl.Datetime("us", "UTC"))` to match our UTC timestamps; and an empty `pl.DataFrame(schema=...)` written via `write_delta(mode="append")` does correctly create the Delta table on disk (verified directly, not obvious from docs). Verified DoD: gold_loan_summary reconciles exactly with silver_loans (494 loans, matching total_amount to the cent, 24 state x risk_band groups); full pipeline rerun yields identical Gold row counts (24 / 0); all 10 existing tests still green.
- 2026-07-15: Phase 3 complete. `pipeline/silver.py` implements the quality gate (pandera `DataFrameSchema` covering loan_id uniqueness, state validity, loan_amount range, application_date not-future, and non-null on 6 key fields = 10 named checks total), masking (`mask_name`: SHA-256 + fixed salt, raw `customer_name` never selected into Silver), `derive_risk_band` (LOW/MEDIUM/HIGH from loan_amount thresholds nudged by loan_type, documented in its own docstring), and light call checks (file-exists implied by landing, transcript non-empty). `pipeline/run_pipeline.py` added (Bronze then Silver; not explicitly bulleted in Phases.md Phase 3 but required for `make pipeline` to work per its own DoD) and Makefile's `pipeline`/`demo` targets switched to `python -m pipeline.run_pipeline` (absolute package imports need `-m`, a plain script path breaks `from pipeline import lake_io`). Same idempotency style as Bronze: Silver checks dq_audit for prior processing of table='loans'/'calls' and skips on rerun, printing this-run's delta (0/0 second time), not a running total. Verified against DoD: fresh full run quarantines exactly the 6 planted defects (LOAN-0050/0150 null state, LOAN-0100 duplicate pair, LOAN-0200 negative amount, LOAN-0400 future date) and nothing else; rerun is idempotent (counts unchanged); `tests/test_silver_quality.py` (6 tests) and `tests/test_masking.py` (4 tests) all green. Added `pyproject.toml` with `[tool.pytest.ini_options] pythonpath = ["."]` so pytest can resolve `from pipeline import silver` from `tests/` (no `__init__.py` files added, namespace packages are fine). Installed `pandera[polars]` 0.32.1. Silenced pandera's spurious "requires pandas >= 2.1.1" UserWarning in silver.py (only the polars backend is used; upgrading the environment's pandas 2.0.3 is out of scope and pandas isn't even an approved dependency) so the live demo console stays clean.
- 2026-07-15: Pandera-polars edge case found while testing (not present in the real 500-row dataset, only in artificial tiny test batches): when *every* row of a column fails a `not_nullable` check, `failure_cases["index"]` comes back `None` instead of a row number, and the reported check name can also differ (observed `"dtype('String')"` instead of `"not_nullable"`). `silver.py`'s `_validate_loans` now falls back to re-scanning for nulls when index is `None`, and uses `dict.get`/`fail_counts.get(...,0)` instead of direct key lookups so an unrecognized check name is quarantined with a generic reason instead of crashing the run (Rules R6, "never crash the run"). This path cannot fire on the real Bronze batch (always 500 rows, defects are always a small fraction), so no dedicated test was kept for it — see [[phase-3-silver]] if this needs revisiting when the dataset shape ever changes.
- 2026-07-15: Phase 3 pandera spike (Phases.md Phase 3, first step), run against real Bronze data (`pandera[polars]` 0.32.1, installed for this phase). Confirmed: range (`Check.in_range`), non-null (`nullable=False`), categorical (`Check.isin`), and uniqueness (`unique=True`) checks all behave as expected on polars DataFrames. Critically, the uniqueness check flags BOTH rows of a duplicate pair as separate `failure_cases` entries (index 99 and 299, both `LOAN-0100`), not just one — confirms Phase 1's arithmetic (duplicate pair = 2 quarantined rows) is correct. `err.failure_cases` gives one row per (column, check, row-index) with a 0-based positional `index` matching the source DataFrame exactly. One gotcha: custom lambda `Check`s receive a `PolarsData` wrapper, not a plain Series/str — use pandera's built-in `Check.le`/`in_range`/`isin` instead of raw lambdas. `application_date` stays a plain string in Bronze (polars' `read_csv` doesn't auto-parse dates), so the future-date check compares against a fixed ISO string `TODAY = "2026-07-15"` (matches generate_data.py's anchor) via `Check.le`, which works because ISO 8601 dates sort lexicographically.
- 2026-07-15: Phase 2 complete. `pipeline/lake_io.py` (`append`/`read`/`table_exists`, wraps polars' native `write_delta`/`read_delta`, no separate deltalake-package calls needed) is the sole lake writer. `pipeline/bronze.py` lands `bronze_loans` (500 rows) and `bronze_calls` (3 rows, one per transcript, `call_id` = filename stem, full `transcript_text` stored) at `data/lake/bronze/`, adding `source_file` and `ingest_timestamp`, no cleaning or masking. Idempotency implemented in bronze.py itself (checks existing `source_file` values before appending) since `lake_io.append` only ever does a raw append. Verified: row counts match source exactly (500/3), re-run lands 0/0 new rows, `customer_name` survives unmasked in Bronze. Run via `python -m pipeline.bronze` (absolute `from pipeline import lake_io` import; no `make bronze` target added, not requested by Phase 2's Makefile scope which was fixed in Phase 1). Added `.gitignore` for `__pycache__/` and `data/lake/` (regenerable pipeline output); `data/landing/` fixtures stay tracked, matching the existing Phase 1 commits.
- 2026-07-15: Phase 2 concurrency spike (Phases.md Phase 2, first step): two separate OS processes (multiprocessing, not threads) writing/reading the same Delta table concurrently via polars' `write_delta`/`read_delta` (25 rounds each, 50ms apart) on this Windows machine. Result: 26/26 expected rows landed, zero lock errors, zero corruption. Decision: keep Delta Lake for all lake storage including Gold, no DuckDB fallback needed. Installed `polars` and `deltalake` (both on Rules.md's allowed list, neither previously installed in this environment) via `pip install polars deltalake`.
- 2026-07-15: Installed `polars`/`deltalake` for Phase 2 needs (see spike entry above); no `requirements.txt` yet, added when packaging (Phase 7) needs a pinned list.
- 2026-07-15: Added `interest_rate` (5.5-14%, base rate per `loan_type` plus jitter, standing in for a risk_band that doesn't exist yet at Bronze) and `channel` (direct/broker, 50/50) to `loans_raw.csv` and to Architecture.md's `silver_loans` contract (both class N, no PII). `risk_band` derivation in Silver is unchanged: still loan_amount/loan_type only, per Phases.md Phase 3 (interest_rate was not wired in as a derivation input). The 6 planted defects are untouched (still LOAN-0050/0150/0200/0100-dup/0400) and reruns remain byte-identical.
- 2026-07-15: Phase 1 complete. `pipeline/generate_data.py` (stdlib only: csv, random, datetime) writes `data/landing/loans_raw.csv` (500 rows, seed 42) and 3 transcripts to `data/landing/transcripts/`. Used a fixed `TODAY` anchor (2026-07-15) instead of `datetime.now()` so output stays byte-identical regardless of the real run date. Verified: 6 planted defects land exactly as spec'd (LOAN-0050/0150 null state, LOAN-0200 negative amount, LOAN-0300 rewritten to duplicate LOAN-0100, LOAN-0400 future-dated), two consecutive runs diff clean. Makefile added with all 6 Phase 1 targets (`data` runs now; `pipeline`/`dashboard`/`service`/`demo`/`test` are stubs pointing at scripts later phases will create).
- 2026-07-15: Plan v2 adopted. Masking added to Silver. Whisper deferred to optional Phase 8. Production concerns documented in ARCHITECTURE_MAPPING.md, not built (Rules R1).
- 2026-07-15: Adversarial review run against PRD/Architecture/Rules/Phases. Fixed 7 real issues: quarantine no longer stores full raw row (PII leak risk), planted defects rebalanced to net exactly 6 quarantined rows, customer_id column dropped entirely (unused), PRD "aggregates only" claim corrected for row-level gold_call_insights, added R3a restricting the AI service demo to the 3 official transcripts only, Docker moved out of the live demo critical path (packaging artifact only), dashboard auto-refresh replaced with a manual Refresh button for presenter control. Also added: Delta-vs-DuckDB concurrency spike moved to Phase 2, pandera-on-polars behaviour spike added to Phase 3, time-overrun fallback order added to Phase 7, fixed-salt-vs-production-HMAC talking point added to Architecture.md.

## Parking lot (good ideas, out of scope)

One line each. Nothing here gets built without the human asking.

- (empty)

## Environment notes

Record Python version, OS, and anything a fresh session must know to reproduce the setup.

- Python 3.11.3, Windows 11. Repo uses `make` (GNU Make must be on PATH) plus plain `python`/`pip`, no venv tooling mandated yet.
